<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:menuCategory="container">
        <item
            android:id="@+id/favorite"
            android:icon="@drawable/favorite_foreground"
            android:title="Favorite"
            ></item>
        <item
            android:id="@+id/List_music"
            android:title="List Music"
            android:icon="@drawable/ic_music_library_foreground"></item>

    </group>
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_listen_now"
            android:icon="@drawable/ic_list_music_foreground"
            android:checkable="true"
            android:checked="true"
            android:title="@string/listen_now" />
        <item
            android:id="@+id/nav_recents"
            android:icon="@drawable/ic_recents_foreground"
            android:checkable="true"
            android:checked="false"
            android:title="@string/recents" />
        <item
            android:id="@+id/nav_favorite_songs"
            android:icon="@drawable/ic_music_library_foreground"
            android:checkable="true"
            android:checked="false"
            android:title="@string/music_library" />
    </group>

    <item android:title="comunicate">
        <menu>
            <item
                android:id="@+id/nav_setting"
                android:title="Settings" />
            <item
                android:id="@+id/nav_help"
                android:title="@string/help_feedback"/>
        </menu>
    </item>

</menu>